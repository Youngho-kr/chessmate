services:
  app:
    image: chess-server
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:7-alpine
    restart: always